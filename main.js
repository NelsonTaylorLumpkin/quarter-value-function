const quarterValue = (crazyNumber) => {
        const quarter = crazyNumber / 4
        return quarter

}
const numberReturned = quarterValue(2002020)
// Invoke the function and store the return value


// Log the return value
console.log(numberReturned)